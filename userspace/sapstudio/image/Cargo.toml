# SPDX-License-Identifier: GPL-3.0-only
#
# The shipping image. Deliberately outside the workspace: it is the only crate
# that cannot be built for the host, because it has no `main`, no standard
# library, and an entry point that only Phipia calls. Keeping it separate means
# `cargo test` at the workspace root never tries.
[package]
name = "sapstudio-image"
version = "0.0.1"
edition = "2024"
rust-version = "1.85"
license = "GPL-3.0-only"
repository = "https://github.com/saudaljuaid/Phipia"
description = "The SapStudio program image for Phipia."

[[bin]]
name = "sapstudio"
path = "src/main.rs"

[dependencies]
sapstudio-app = { path = "../crates/sapstudio-app" }
sapstudio-rt = { path = "../crates/sapstudio-rt" }

[profile.release]
panic = "abort"
opt-level = 2
lto = false
codegen-units = 1
overflow-checks = true
debug = true

[profile.dev]
panic = "abort"
overflow-checks = true

[lints.rust]
warnings = "deny"
missing_docs = "deny"
